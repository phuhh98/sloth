---
sidebar_position: 2
---

# Architecture

This page describes how the major pieces of sloth fit together at a high level.

## System Diagram

```mermaid
flowchart LR
  %% Actors
  A[Admin User]
  D[Frontend Runtime Consumer]
  E[Developer]

  %% Local developer workspace
  subgraph L[Local Project]
    C[sloth CLI\nGo + Cobra]
    F[.sloth config and contracts\nconfig.yaml, contracts, sets, lock.json]
  end

  %% Host system
  subgraph H[Strapi Host with sloth plugin]
    B[Admin UI Builder\nPuck + palette + dataset mapping]
    R1[Admin API\ncomponents/pages/compile]
    R2[Content API\ninspection + ingest + page delivery]
    S[Plugin Services\ningest, inspection, compiler, sync]
    T1[plugin::sloth.component]
    T2[plugin::sloth.page]
    G[(Strapi Document Service API)]
  end

  %% Distribution ecosystem
  subgraph X[Distribution Ecosystem]
    K1[Docusaurus Registry Source\napps/docs/static/registry]
    K2[Versioned Contract Packages\nfuture: @sloth/* on npm]
    U[Component Hub\nthemes and variants]
    V[Registry API and Artifacts\nfuture]
  end

  %% Admin flow
  A --> B
  B --> R1
  R1 --> S
  S --> G
  G --> T1
  G --> T2

  %% Runtime flow
  D --> R2
  R2 --> S
  S --> G
  R2 --> D

  %% CLI flow
  E --> C
  C --> F
  C --> K1
  C --> R2

  %% Verification rule
  C -. verifies schema, compatibility, and drift before push .-> R2
  R2 -. ingests verified payloads only .-> S

  %% Future ecosystem links
  U -. publishes versioned contract bundles .-> K2
  C -. can pull versioned contracts .-> K2
  C -. list/add/update/search .-> V
  U -. publishes packs to .-> V
```

## Contract Source and Versioning

- Current source of contract artifacts is the Docusaurus registry files under `apps/docs/static/registry`.
- Future distribution/version-control path can publish versioned `@sloth/*` contract packages to npm.

## Actors

**Admin User** — uses the Puck drag-and-drop builder embedded in the Strapi admin panel to compose page templates from available component slots and bind them to CMS content.

**Developer** — uses the sloth CLI to author component contracts locally, validate them against the hosted JSON Schema, and push them to a Strapi host.

**Frontend Runtime Consumer** — a frontend application (Next.js, Nuxt, etc.) that fetches page delivery payloads from the Strapi Content API and renders them using its own component implementations.

## Responsibility Boundaries

| Layer                                              | Owned By                              |
| -------------------------------------------------- | ------------------------------------- |
| Content-types (`component`, `page`)                | Strapi plugin                         |
| Contract ingestion and validation                  | Plugin services (ingest + inspection) |
| Page composition UI                                | Admin UI (Puck builder)               |
| Page compilation (`puckConfig` → `compiledConfig`) | Plugin compiler service               |
| Contract authoring and local validation            | sloth CLI                             |
| Public schema artifacts                            | This documentation site               |
| Reusable component variants                        | Component Hub (future)                |
| Public/private registry                            | Registry API (future)                 |

## Data Flow: Contract Ingestion

1. Developer authors a contract in YAML/JSON locally.
2. CLI validates the contract against the hosted JSON Schema.
3. CLI pushes the validated payload to the Content API `/sloth/ingest` endpoint.
4. Plugin ingestion service normalizes and writes a `component` record via the Document Service API.
5. The Admin UI reflects the new component in the builder palette.

## Data Flow: Page Delivery

1. Frontend runtime requests a page by document ID from the Content API.
2. Plugin returns the page record including its compiled component config and bound content references.
3. Frontend resolves component slots using its own renderer registry keyed by `rendererKey`.
